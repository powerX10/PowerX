rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Passkeys/challenges are server-admin only.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
