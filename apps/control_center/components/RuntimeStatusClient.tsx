"use client";import{useEffect,useState}from"react";
export default function RuntimeStatusClient({settings=false}:{settings?:boolean}){const[d,setD]=useState<any>(null),[loading,setL]=useState(true);async function load(){try{setD(await(await fetch("/api/runtime/status",{cache:"no-store"})).json())}finally{setL(false)}}useEffect(()=>{load();const t=setInterval(load,10000);return()=>clearInterval(t)},[]);if(settings)return <div className="grid2"><div className="cardx"><h3>Routing policy</h3><p>Healthy phone first, then CPU server, then optional GPU.</p></div><div className="cardx"><h3>Broker</h3><p>Vercel + Firebase Firestore. Modal is not required.</p></div><div className="cardx"><h3>Heartbeat</h3><p>Nodes are healthy when they checked in during the last 45 seconds.</p></div><div className="cardx"><h3>GPU</h3><p>Optional for heavy image/video workloads.</p></div></div>;if(loading&&!d)return <div className="skeleton"/>;const n=d?.nodes||[];return <div className="nodegrid">{n.length?n.map((x:any)=><div className="cardx" key={x.id}><div className="nodehead"><div><strong>{x.name||x.id}</strong><small>{x.runtime_class}</small></div><span className={"badge "+(x.healthy?"ok":"off")}>{x.healthy?"Online":"Offline"}</span></div><div className="kv"><span>Models</span><b>{x.models?.length||0}</b></div><div className="chips">{(x.models||[]).slice(0,6).map((m:string)=><span key={m}>{m}</span>)}</div></div>):<div className="cardx emptyx"><h3>No runtime node online</h3><p>Start the Android/Termux node or the CPU server node. The PowerX UI remains online independently.</p></div>}</div>}
