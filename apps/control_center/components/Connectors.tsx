"use client";import{useEffect,useState}from"react";export default function Connectors(){const[items,setItems]=useState<any[]>([]);const[name,setName]=useState("");const[baseUrl,setBaseUrl]=useState("");const[secret,setSecret]=useState("");async function load(){setItems((await(await fetch("/api/powerx/connectors")).json()).items||[])}useEffect(()=>{load()},[]);async function add(){await fetch("/api/powerx/connectors",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name,baseUrl,secret,enabled:true})});setName("");setBaseUrl("");setSecret("");load()}async function del(id:string){await fetch("/api/powerx/connectors?id="+id,{method:"DELETE"});load()}return <><div className="card" style={{padding:20}}><h3>Add tool / connector</h3><p style={{color:"#8d9aab"}}>HTTPS connector registry. OAuth services still require their own developer credentials.</p><input className="input" placeholder="Name" value={name} onChange={e=>setName(e.target.value)}/><input className="input" style={{marginTop:8}} placeholder="HTTPS base URL" value={baseUrl} onChange={e=>setBaseUrl(e.target.value)}/><input className="input" style={{marginTop:8}} type="password" placeholder="Optional secret" value={secret} onChange={e=>setSecret(e.target.value)}/><p><button className="btn btnPrimary" onClick={add}>Save connector</button></p></div>{items.map(x=><div className="card" style={{padding:16,marginTop:10,display:"flex",justifyContent:"space-between"}} key={x.id}><div><strong>{x.name}</strong><div>{x.baseUrl}</div></div><button className="btn" onClick={()=>del(x.id)}>Remove</button></div>)}</>}
