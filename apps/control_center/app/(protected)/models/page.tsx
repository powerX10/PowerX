import {PageHead} from "@/components/PageHead";import {AsyncPanel} from "@/components/AsyncPanel";
export default function Page(){return <><PageHead title="Models" subtitle="Installed and registered AI models."/><AsyncPanel url="/api/powerx/status">{d=><div className="grid">{(d.models||[]).map((m:any)=><div className="card span4" key={m.id}><div className="row"><b>{m.name||m.id}</b><span className="pill"><span className={"dot "+(m.healthy?"":"off")}/>{m.healthy?"Healthy":"Offline"}</span></div><p className="muted small">{m.role||m.runtime}</p><div className="small">Runtime: {m.runtime||"—"}</div></div>)}</div>}</AsyncPanel></>}
