import Topbar from"@/components/Topbar";import{control}from"@/lib/powerx";export default async function Page(){const d:any=await control("/status");return <><Topbar title="Models" subtitle={`${d?.registered_models??0} registered models`}/><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(260px,1fr))",gap:12}}>{(d?.models||[]).map((m:any)=><div className="card" key={m.id} style={{padding:17}}><strong>{m.name||m.id}</strong><div style={{color:"#8d9aab"}}>{m.runtime} · {m.adapter}</div><div>{(m.capabilities||[]).map((x:string)=><span className="attachmentChip" key={x}>{x}</span>)}</div></div>)}</div></>}