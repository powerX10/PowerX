import {PageHead} from "@/components/PageHead"; import {AsyncPanel} from "@/components/AsyncPanel";
export default function Page(){return <><PageHead title="Command Center" subtitle="Models, runtimes and PowerX health at a glance."/><div className="grid"><AsyncPanel url="/api/powerx/status">{d=><>{["gpu","cpu","mobile"].map(k=><div className="card span4" key={k}><div className="label">{k.toUpperCase()} runtime</div><div className="metric">{d[k]?.running??0}</div><div className="muted small">{d[k]?.detail||"Ready for runtime data"}</div></div>)}</>}</AsyncPanel><div className="card span12"><div className="row"><div><b>PowerX architecture</b><div className="muted small">Zerion X1 → PowerX router → best healthy AI runtime → response</div></div><span className="pill"><span className="dot"/>Automatic</span></div></div></div></>}
