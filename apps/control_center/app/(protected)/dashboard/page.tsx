import Topbar from"@/components/Topbar";import{control}from"@/lib/powerx";export default async function Dashboard(){const s:any=await control("/status");const r=s?.runtimes||{};return <><Topbar title="Command Center" subtitle="Live PowerX runtime overview"/><div className="dashboardGrid"><div className="card stat"><span>Models</span><strong>{s?.registered_models??20}</strong></div><div className="card stat"><span>Assistant</span><strong>{s?.assistant??"MA"}</strong></div><div className="card stat"><span>CPU</span><strong>{r?.modal_cpu?.healthy?"Online":"Standby"}</strong></div><div className="card stat"><span>Broker</span><strong>{r?.runtime_broker?.healthy?"Online":"Standby"}</strong></div></div></>}
