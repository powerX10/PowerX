import{redirect}from"next/navigation";import{hasSession}from"@/lib/session";import AppSidebar from"@/components/AppSidebar";export default async function Layout({children}:{children:React.ReactNode}){if(!(await hasSession()))redirect("/login");return <div className="appGrid"><AppSidebar/><main className="main">{children}</main></div>}