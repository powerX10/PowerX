import Topbar from"@/components/Topbar";import{control}from"@/lib/powerx";export default async function Page(){const d:any=await control("/status");const r=d?.runtimes||{};return <><Topbar title="Runtimes" subtitle="Automatic execution fabric"/><div className="dashboardGrid">{Object.entries(r).map(([k,v]:any)=><div className="card stat" key={k}><span>{k.replaceAll("_"," ")}</span><strong>{v?.healthy?"Online":v?.configured?"Ready":"Optional"}</strong></div>)}</div></>}