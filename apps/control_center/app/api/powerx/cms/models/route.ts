import{NextRequest,NextResponse}from"next/server";import{hasSession}from"@/lib/session";import{db,COL}from"@/lib/powerx-db";
export async function GET(){if(!(await hasSession()))return NextResponse.json({error:"Unauthorized"},{status:401});const s=await db().collection(COL.models).get();return NextResponse.json({items:s.docs.map(d=>({id:d.id,...d.data()}))})}
export async function POST(req:NextRequest){if(!(await hasSession()))return NextResponse.json({error:"Unauthorized"},{status:401});const b=await req.json();if(!b.id)return NextResponse.json({error:"id required"},{status:422});await db().collection(COL.models).doc(String(b.id)).set({...b,updatedAt:Date.now()},{merge:true});return NextResponse.json({ok:true})}
export async function DELETE(req:NextRequest){if(!(await hasSession()))return NextResponse.json({error:"Unauthorized"},{status:401});const id=new URL(req.url).searchParams.get("id");if(!id)return NextResponse.json({error:"id required"},{status:422});await db().collection(COL.models).doc(id).delete();return NextResponse.json({ok:true})}