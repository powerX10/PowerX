import{NextRequest,NextResponse}from"next/server";import{hasSession}from"@/lib/session";import{getAdminDb}from"@/lib/firebase-admin";
export async function POST(req:NextRequest){if(!(await hasSession()))return NextResponse.json({error:"Unauthorized"},{status:401});const b=await req.json();if(!b.node_id||typeof b.enabled!=="boolean")return NextResponse.json({error:"node_id and enabled required"},{status:422});await getAdminDb().collection("powerx_runtime_nodes").doc(String(b.node_id)).set({desired_enabled:b.enabled,control_updated_at:Date.now()},{merge:true});return NextResponse.json({ok:true,node_id:b.node_id,enabled:b.enabled})}
