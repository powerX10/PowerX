# PowerX Fix Phase
- Full 20-model editable CMS.
- Runtime pause/resume controls.
- Mobile watchdog + wake lock.
- Clean error/loading boundaries.
- Phone-first routing; CPU fallback; GPU optional.
- All 20 models stay enabled/available by default, but are loaded on demand rather than all in RAM at once.
